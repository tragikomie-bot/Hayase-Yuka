# 邮箱记账 1.2.0

这次我记住啦，公告只说一次，不会每次打开都拉着你重复念叨。想再听，去设置里找我就好。

国外汇率接口 Frankfurter 也准备好了：设置 → 自动汇率 → 汇率来源，选择国外接口，保存后就能查询，不需要 AppKey。你负责好好生活，我帮你把不同币种的账算清楚。

该省的地方一起留心，该吃的饭、该照顾自己的钱也别舍不得。你的钱由你做主，花得明白，也要花得开心。

首次展示前将状态同步写入 Android SharedPreferences，之后重开不自动弹出，不依赖关闭按钮或 WebView localStorage。清除应用数据或卸载后重新安装会重新显示。设置可主动查看。

国外接口查询最新可用/历史参考汇率，并显示实际日期。休市或更新延迟可能返回旧日期，手工记账需确认；定时规则仍严格要求日期匹配，否则等待重试，可用固定汇率或跳过本次。国内服务仍保留，默认不自动切换。国外接口不发送AppKey、账目金额或备注。接口缓存按来源区分，不影响已保存账目。

接口文档：https://frankfurter.dev/ 。开发环境实测查询成功，但未验证中国各运营商网络。版本1.2.0，内部版本号4，包名和签名保持不变。
